# 项目复盘

## 2026-08-30：构建不应强依赖 Git 元数据

从源码压缩包构建时不存在 `.git`，原有 APK 命名逻辑直接执行
`git rev-parse` 并因退出码 128 终止整个 Gradle 配置阶段。读取 Git 哈希时必须忽略非零退出码，
并在无 Git 元数据时使用稳定的 `local` 后缀。

## 2026-08-31：源码上传审查必须覆盖截图和 Windows 文件名

纯文本密钥扫描无法发现截图中的用户名、头像、账户统计、搜索历史等隐私信息，上传图片前必须人工检查；
非必要的界面截图应保留在本机并通过 `.gitignore` 排除。Windows 上读取 `git ls-files` 时还应设置
`core.quotepath=false`，否则中文文件名会被转义并导致审查脚本误判为非法路径。
