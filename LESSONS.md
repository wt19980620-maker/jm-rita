# 项目复盘

## 2026-08-30：构建不应强依赖 Git 元数据

从源码压缩包构建时不存在 `.git`，原有 APK 命名逻辑直接执行
`git rev-parse` 并因退出码 128 终止整个 Gradle 配置阶段。读取 Git 哈希时必须忽略非零退出码，
并在无 Git 元数据时使用稳定的 `local` 后缀。

## 2026-08-31：源码上传审查必须覆盖截图和 Windows 文件名

纯文本密钥扫描无法发现截图中的用户名、头像、账户统计、搜索历史等隐私信息，上传图片前必须人工检查；
非必要的界面截图应保留在本机并通过 `.gitignore` 排除。Windows 上读取 `git ls-files` 时还应设置
`core.quotepath=false`，否则中文文件名会被转义并导致审查脚本误判为非法路径。

## 2026-09-01：下载产物必须接入离线阅读链路

仅创建下载任务或生成 ZIP 并不代表下载功能可用；如果阅读页仍只请求网络，用户离线时仍无法阅读。
下载页面应先完成图片解码并保存到应用私有持久目录，阅读页优先读取已完成的本地页面，同时避免将大体积下载内容纳入系统备份。

## 2026-09-01：发布页网站域名与原生 API 线路不可混用

永久发布链接解析得到的是当前网站域名，而 App 的 `/setting` 等原生接口使用独立 API 线路；网站域名直接写入 API 拦截器会导致请求 404。
域名自动发现应独立保存当前内容站点，失败时沿用上次结果，API 线路继续由 `LocalSetting` 统一维护。

## 2026-09-01：线路健康检查不能只判断 HTTP 200

部分失效 API 域名仍会返回状态码 200，但正文是 Cloudflare 或普通网页；健康检查必须同时验证 JSON 类型和 API 成功码。
对于已解码并持久化的下载图片，阅读时应直接读取本地文件，避免再次经过网络图片加载器并重复写入缓存。

## 2026-09-01：Release 签名配置必须在编译前验证

发布工作流引用了签名 Secret 不代表仓库已经配置这些值；空 Secret 会让耗时的 Release 编译直到打包阶段才失败。
工作流应在编译前检查所有签名变量，并在安全位置长期备份同一把发布密钥；更换或丢失密钥会导致已安装用户无法覆盖升级。

## 2026-09-01：第三方视频元数据与播放链路应分开

Pornhub Webmaster 接口可提供列表、搜索、封面和预览帧等公开元数据，但不提供稳定的原生播放流；网页内解析出的临时流地址容易过期，也会增加合规风险。
原生客户端可用接口返回的预览帧实现动态预览，并把完整播放交给内容来源的官方页面，不引入 WebView、下载器或页面抓取器。

## 2026-09-02：临时媒体地址必须在播放前解析

网页中的 HLS/MP4 地址通常带有过期签名，不能作为视频元数据长期缓存；应只保存视频 ID，并在用户点击播放时重新解析。
解析嵌入页面的 `mediaDefinitions` 时要按 JSON 括号平衡截取，避免普通正则被 URL 或嵌套内容截断；播放器请求还要携带来源页 `Referer`。
Media3 的实验 API 要使用 `androidx.annotation.OptIn` 标记，Kotlin 自带的 `OptIn` 不会满足 Android Lint。
